<% unless @startup.user == current_user %>
	<div id="request_form">
	<% if current_user.requested?(@startup) %>
		<%= render "invitations/cancel_request" %>
	<% else %>
		<%= render "invitations/request_invite" %>
	 <% end %>
	</div>
<% end %>
