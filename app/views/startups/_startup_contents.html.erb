		
	<div class="tabbable">
		<ul class="nav nav-pills" id="updateTabs">
			<li class="active"><a href="#pitch" data-toggle="pill"><b>Updates</b></a></li>
			<li><a href="#updates" data-toggle="pill"><b>Pitch</b></a></li>
		</ul>
			<div class="tab-content">
			<div class="tab-pane active" id="pitch">										
			<% if signed_in? %>
			  <% if current_user == @startup.user || current_user.admin? %>
				<%= render "updates/startup_updates" %>
			  <% end %>
			<% end %>
  			<%= render 'updates/updates' %>
			</div>
			<div class="tab-pane" id="updates">
			<div>
			<p><%= @startup.pitch %></p>
			<div class="muted">
				By <%= link_to @startup.user.username, user_path(@startup.user) %>
			</div>
		  </div>
		</div><!-- /.tab-content -->
	</div><!-- /.tabbable -->

